# Classifer

A basic image classifier based on the concept of neural network using PyTorch.

Use the command below to create a virtual environment for the development.

    clear ; conda create -yn test python=3.13
    clear ; conda activate test
    clear ; pip install -r requirements.txt

Use the commands to run the project.

    clear ; python train.py
    clear ; python infer.py